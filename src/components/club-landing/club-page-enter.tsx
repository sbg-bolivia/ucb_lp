/** Entrada de página con CSS puro — sin JS de animación. */
export function ClubPageEnter({ children }: { children: React.ReactNode }) {
  return <div className="club-page-enter">{children}</div>;
}
